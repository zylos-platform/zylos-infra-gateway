/**
 * Gateway-side RFC 8693 token exchange: downscopes the ingress token's audience
 * to a per-service audience and records the gateway as the delegation actor.
 */
@NullMarked
package app.zylos.gateway.exchange;

import org.jspecify.annotations.NullMarked;
